// Single source of truth for the API base URL. Revisit this with Angular's
// environment files once Vercel/Render deployment is actually wired up —
// not worth the fileReplacements setup while everything runs locally.
export const API_BASE_URL = 'http://localhost:3000';
